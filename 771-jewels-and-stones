class Solution:
    def numJewelsInStones(self, J: str, S: str) -> int:
        setJ = set(J)
        count = 0
        for i in S:
            if i in setJ:
                count += 1
        return count
